# Subscription Upgrade And Downgrade Guide

Plan changes are tracked by `PlanChangeRequest`.

Paid features unlock only after provider verification. Downgrades must preserve tenant data, including mobile app, ePaper, video, ads, themes, staff, and storage configuration.

When a tenant exceeds a downgraded limit, do not delete data automatically. Prevent new additions and guide the customer to resolve the over-limit state.
