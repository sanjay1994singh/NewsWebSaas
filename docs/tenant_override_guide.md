# Tenant Override Guide

Use `TenantFeatureOverride` in Django Admin to grant, restrict, or configure a feature for one tenant.

Overrides are useful for pilots, manual support resolutions, temporary restrictions, and enterprise exceptions.

Always add a reason and expiration date when the override is temporary.
